meta:
  environment: python
  toolchain:
    name: pip
    version: 3.10
build:
  requirementsPath: requirements.txt
run:
  command: gunicorn beauty.wsgi:application  --bind 0.0.0.0:80 && python manage.py launch_bot
  persistenceMount: /data
  containerPort: 80
